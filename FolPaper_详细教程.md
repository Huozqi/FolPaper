# FolPaper 详细使用教程

FolPaper 是一款专为科研人员、学者及学生打造的强大文献订阅、阅读与翻译辅助工具。本文档将按照系统左侧导航菜单的自上而下的顺序，为您详细拆解每个功能模块的作用、核心功能及使用方法。

---

## 🌟 FolPaper 能做什么？

FolPaper 将繁杂的文献收集与阅读过程转化为现代化的 AI 辅助工作流。使用本应用，您可以：

1. **聚合学术资讯**：一站式管理 RSS、PubMed、OpenAlex 和 arXiv 订阅源，自动将最新发表的学术文献推送到您的收件箱。
2. **打破语言壁垒**：利用大语言模型（LLM），支持一键批量翻译海量英文标题，以及对文献的标题和摘要进行学术级精准翻译。
3. **高效知识管理**：通过文库、历史归档及自动清理策略，轻松管理和找回具有高价值的科研文献。
4. **AI 智能调研与筛选**：
   * 用大白话指令让 AI 从您的文库、历史文献或本地导入的文件中精选高度相关的文献，逐篇给出命中/排除判定与推荐理由。
   * 在 PubMed 检索中，只需输入主题，AI 即可帮您构建检索式、并发筛选数百篇文献摘要，并自动生成一篇带有标准学术引用的高质量中文背景调研报告。
   * 内置顶级学术会议库（NeurIPS、CVPR、ACL 等），一键拉取会议论文并按主题 AI 筛选。
   * 文献详情页支持通过 DOI 自动定位并下载 PDF 全文。

---

## ⚙️ 使用前需要配置什么？

FolPaper 的核心智能化能力完全依托于外部大语言模型。因此，在开始使用所有 AI 功能（如标题翻译、全文翻译、AI 洞察筛选、生成调研报告）之前，您**必须**先完成以下配置：

1. 打开应用后，点击首页下方导航栏的 **[设置]**（或在任意子页面顶部点击"设置"）。
2. 填入兼容 OpenAI 接口格式的 **Base URL**（支持官方或第三方中转服务商，如 `https://api.deepseek.com/v1` 或阿里云百炼兼容端点）。思考模式开关会**按服务商自动适配**（百炼/DashScope 使用 `enable_thinking`，DeepSeek 使用 `thinking: {type}`），切换服务商无需改代码。
3. 填入**API 密钥 (API Key)** 及对应的**模型名称**。百炼推荐使用 `qwen3.6-flash`：成本极低且有赠送额度，每日 400 篇文献规模的获取数量下，日消耗约 0.1 元。也可以分别为翻译、推荐/筛选、调研指定专用模型（留空则使用默认模型）。
4. 点击"测试 API 连通性"，提示成功后点击"保存设置"，即可开启您的智能学术之旅！

> 思考模式默认关闭（`off`），以降低成本并提高并发速度；可在 设置 → 生成参数 → 思考模式 中切换为 `auto`（由模型自行决定）或 `on`（强制思考，token 消耗显著增加）。

*不使用该配置，仍然可追踪文献进展，只不过没有翻译、筛选等功能。*

---

## 1. 首页 (Home)

### 📌 功能概述
应用启动后进入的仪表盘页面，以品牌首页的形式集中展示当前文献管理的关键数据与快捷入口。

### ⚙️ 详细功能介绍
*   **KPI 数据看板**：顶部展示四项实时统计——今日新增文献数、未读文献总数、订阅源数量、近 7 天新增文献数。每项卡片均可点击，直接跳转到对应模块（如点击"未读文献"进入最新进展页面）。
*   **模块导航**：以编号卡片（01~07）呈现七大核心模块：最新进展、文献探索、期刊抓手、学术前沿、AI 洞察、研究文库、时间档案；下方另有订阅源、设置、关于三个辅助入口。

### 💡 使用示例
每天早晨打开 FolPaper，先看首页 KPI：今日新增 12 篇、未读 87 篇。点击"未读文献"卡片，进入收件箱开始筛选。

---

## 2. 最新进展 (Inbox)

### 📌 功能概述
您的**文献收件箱**。系统会定期通过 RSS 或 API 获取您订阅的最新文献，并汇聚于此。在这里，您可以快速浏览最新发表的文章标题，并决定是深度阅读、保存还是忽略。

### ⚙️ 详细功能介绍
*   **期刊源树形菜单**：侧边栏的"最新进展"下方会展开您当前订阅的所有独立期刊源（如 Nature, Science 等）。每个源旁边带有红色的未读数量角标，点击即可独立查看该期刊的最新文章。
*   **一键翻译未读标题**：面对海量英文标题，点击顶部操作栏的"翻译所有标题"，系统将并发调用 AI 模型，将当前列表内的所有英文标题转换为中文，极大提升筛选效率。翻译进度实时显示，未读数同步扣减。
*   **文献状态流转**：
    *   **保存至文库 (⭐)**：将具有极高阅读价值的文献永久保存至"研究文库"。
    *   **归档 (🗄️)**：已读但不需要永久保存的文献，可归档备查。
    *   **删除 (🗑️)**：对完全不感兴趣的文献进行清理。
*   **一键已读**：快速将当前列表的所有文献标记为已读，消除红点提示。
*   **手动更新订阅源**：在特定期刊页面，可点击"更新此订阅源"强制后台立即拉取最新数据。
*   **清空列表**：一键清空当前列表中的全部待处理文献（需二次确认）。

### 💡 使用示例
每天早晨打开 FolPaper，点击左侧的"最新进展"。看着满屏的英文标题，点击右上角的"翻译所有标题"。快速扫视中文标题后，发现三篇非常契合您当前课题的文章，依次点击它们卡片上的"保存至文库"。最后，点击"一键已读"，清空今日收件箱。

### ⚠️ 注意事项
*   收件箱中的文献**非永久保存**。系统会根据各订阅源或"系统设置"中配置的"保留天数"自动清理过期且未处理的文献。对于有价值的文献，请务必操作"保存至文库"。

---

## 3. 文献探索 (PubMed)

### 📌 功能概述
针对生物医学领域的"核武器"。不仅支持常规的 PubMed 检索，还深度融合了 AI 能力，实现从"检索式构建"到"自动撰写背景调研综述"的全流程自动化。

### ⚙️ 详细功能介绍
*   **AI 辅助生成检索式**：不知道怎么写复杂的 MeSH 词？点击"AI 辅助生成"，输入"糖尿病的最新免疫疗法"，AI 即可为您生成专业的 PubMed 检索式并填入检索框。支持**自然语言**和**关键词**两种输入模式。
*   **手动构建检索式**：支持多行检索词（自动用 AND 连接），亦可直接编辑最终的检索表达式（如 `cancer[Title/Abstract] AND 2023[PDAT]`）。
*   **直观的检索结果展示**：拉取并解析 PubMed 数据，直观展示标题、期刊、作者及摘要；支持单篇/批量标题翻译、摘要翻译。
*   **保存与追踪**：可将感兴趣的结果保存到文库；点击"追踪研究进展"可将当前检索式保存为订阅源，此后系统会定期拉取该检索式下的最新文献并推送至您的收件箱。
*   **一键生成 AI 调研报告**：
    *   **自动并发鉴别**：系统会先将检索到的数百篇文献摘要并发交给 AI，剔除无关文献。
    *   **深度撰写综述**：基于筛选出的高度相关文献，AI 会撰写一篇详实、带有学术引用标号（如 `[1], [2]`）的背景调研报告。
    *   **报告导出**：支持一键复制报告（Markdown 格式）、下载 Markdown 文件，以及导出报告引用文献的 CSV。
*   **引用数据下载**：生成的报告下方附带参考文献列表。

### 💡 使用示例
在检索框输入 `gvp[Title/Abstract] AND ADMET[Title/Abstract]`，点击检索获取结果。浏览一遍后，点击页面下方的"生成 AI 调研报告"。系统会自动筛选出相关的文章，并生成一篇包含"研究现状"、"存在问题"及带有标准引用的高质量中文综述。如果您希望以后每天都能看到该领域的新发文献，点击"追踪研究进展"将其保存为自动追踪源，极大节省了日后重复检索的时间。

### ⚠️ 注意事项
*   生成 AI 调研报告的过程包含"多线程筛选"和"长文本生成"两个阶段，通常需要耗时 30 秒至 2 分钟，请耐心等待。
*   报告的质量高度依赖于您的检索式所召回文献的相关性与质量。

---

## 4. 期刊抓手 (OpenAlex)

### 📌 功能概述
依托于强大的 OpenAlex 开放学术图谱，FolPaper 允许您绕过传统的 RSS 限制，直接按期刊名和时间段，精准、全量地拉取过往文献。

### ⚙️ 详细功能介绍
*   **期刊与时间段精准检索**：输入准确的期刊名称（如 `Journal of Medicinal Chemistry`，支持多个期刊逗号分隔）及起止日期，即可拉取该时间段内的所有文章元数据。
*   **可视化统计图表**：拉取完成后，页面会自动渲染精美的统计图表，展示所选期刊在时间段内的发文量分布。
*   **AI 批量二次筛选**：拉取到大量文献后，可以在下方输入特定 Topic（如"肿瘤微环境"），系统会并发调用 AI，根据每篇文章的标题和摘要判断相关性。
*   **数据导出**：筛选完毕后，支持一键将结果导出为 `.xlsx` (Excel) 格式，包含标题、DOI、摘要及 AI 判定结果，方便本地存档分析。
*   **可选 OpenAlex API Key**：如需更高调用限额，可在此填入 OpenAlex API Key（无 key 亦可使用）。

### 💡 使用示例
导师要求您调研 `Cell` 期刊 2023 年全年发表的所有关于 `CAR-T` 细胞疗法的文章。您在该页面输入 `Cell` 和时间范围，拉取了 800 篇文章。接着在 AI 筛选框输入 `CAR-T 疗法`。筛选完成后，点击"导出 Excel"，将生成的表格直接发送给导师。

### ⚠️ 注意事项
*   OpenAlex 接口偶尔存在网络波动，拉取大量数据时可能需要等待十秒至半分钟。
*   AI 筛选进度条实时更新，中途请勿刷新页面，否则可能中断筛选进程。

---

## 5. 学术前沿 (Top Conferences)

### 📌 功能概述
内置全球顶级学术会议库，一键按会议和时间段拉取论文，并对计算机等热门领域（AI/ML、CV、NLP、数据挖掘等）的顶会论文进行主题筛选。

### ⚙️ 详细功能介绍
*   **内置顶会目录**：预置 NeurIPS、ICML、ICLR、AAAI、IJCAI、CVPR、ICCV、ECCV、ACL、EMNLP、NAACL、SIGIR、WWW、KDD、ICDE、VLDB 等主流会议，按领域分组，勾选即可。
*   **时间段与数量控制**：设置起止日期和最大拉取数量（默认 300 篇）。
*   **AI 主题筛选**：拉取后输入 Topic，系统并发调用 AI 筛选相关性文献，并统计命中/排除结果。
*   **数据导出**：筛选结果支持导出 Excel，便于整理参考文献。

### 💡 使用示例
写机器学习综述前，勾选 NeurIPS、ICML、ICLR 三个会议，时间范围设为近两年，点击拉取。拿到约 900 篇论文后，输入"面向大语言模型的高效微调方法"，AI 为您挑出 23 篇高度相关论文，一键导出 Excel 备用。

### ⚠️ 注意事项
*   OpenAlex 对部分会议源收录可能存在延迟，若拉取结果为空，可尝试扩大时间范围。
*   可选填入 OpenAlex API Key 以获得更高限额。

---

## 6. AI 洞察 (Recommend)

### 📌 功能概述
当您积累了大量文献却无暇逐篇阅读时，该功能利用大语言模型（LLM），根据您输入的自然语言要求，对指定的文献集合进行**逐篇深度判定**：每篇文献都会得到"命中 / 排除 / 失败"三种状态之一，命中的文献附带推荐理由。

### ⚙️ 详细功能介绍
*   **自定义自然语言筛选**：支持输入类似"帮我寻找使用深度学习进行蛋白质结构预测的最新研究"这样的大白话指令。
*   **多数据源支持**：
    *   **本地文献**：可勾选"研究文库"或"时间档案"中的文献作为分析样本，并按阅读状态（仅未读 / 仅已读 / 全部）和发表日期范围进一步收窄范围。
    *   **外部导入**：支持直接上传 Web of Science (WoS) 导出的 `.xls` / `.xlsx` / `.csv` 文件（需包含 `Article Title`、`Abstract`、`DOI` 列）进行独立分析。
*   **逐篇精细判定**：AI 逐篇精读摘要，输出命中/排除判定、方向与置信度、推荐理由，统计"推荐 N 篇、排除 M 篇、失败 K 篇"。
*   **失败自动重试**：单篇判定失败的文献自动降并发重试多轮，尽量降低遗漏。
*   **结果管理**：命中结果支持在结果内**关键词搜索**、按**置信度过滤**、按字段**排序**、分页浏览，并支持批量/单篇标题翻译。
*   **导出**：一键导出筛选结果（CSV 格式），包含标题、DOI、摘要及 AI 判定信息。

### 💡 使用示例
在准备撰写开题报告时，您将"研究文库"中积累的 100 篇未读文献选为数据源。在筛选要求中输入"筛选出所有与 CRISPR 靶向脱靶效应相关的文章，并总结它们的改进方法"。喝杯咖啡后，AI 为您精准挑选出了 8 篇文章，并附带了详细的推荐理由。不满意时，还可以在结果框内继续搜索关键词微调判断。

### ⚠️ 注意事项
*   逐篇判定模式下，AI 需要进行大量的 Token 消耗，请确保您的 API 账户拥有充足的余额；"推荐并发数"可在设置中调整（默认 20），并发过高可能触发 API 限流。
*   失败重试会额外消耗时间，全部失败时页面会提示检查模型配置或 API 限额。

---

## 7. 研究文库 (My Library)

### 📌 功能概述
这是 FolPaper 的**核心文献管理中枢**。所有被您标记为"保存"的重点文献都会永久存放在这里，不受自动清理规则的限制。

### ⚙️ 详细功能介绍
*   **详情阅读**：点击文献卡片，进入详情页。可查看完整的作者列表、发表时间、DOI（支持一键跳转原文链接）以及完整的摘要信息。
*   **全文翻译 (AI驱动)**：在文献详情页，点击"翻译"按钮，系统将调用大语言模型对**标题和摘要**进行高质量、符合学术习惯的精准中文翻译，并以优美的排版展示在原文下方。
*   **PDF 全文下载**：点击"下载全文"按钮，系统将**通过 DOI / 原文链接 / arXiv ID 自动定位 PDF 并下载**到本地（可在设置中指定下载目录）。下载为异步任务，可实时查看进度；集成下载重试机制，自动绕过部分网站的防爬限制。
*   **关注状态**：对重要文献可"关注全文"，系统会自动跟踪其后续引用与版本更新。
*   **状态与排序**：可将文献标记为"未读"或"已读"；支持按照阅读状态、保存时间排序，支持按期刊源过滤文库内容。

### 💡 使用示例
进入"研究文库"，点击一篇昨日保存的重磅论文进入详情页。点击"下载全文"，十几秒后 PDF 已保存到本地下载目录；再点击"翻译"，通顺流畅的中文摘要呈现出来，帮助您迅速掌握了文章的核心创新点。

### ⚠️ 注意事项
*   文库中的文献是永久保存的，只有当您手动点击"移出文库"或"彻底删除"时才会消失。
*   PDF 下载成功率取决于 DOI 可解析性及目标出版社的访问限制，下载失败时可尝试更换 DOI 解析来源或使用全文翻译应急。

---

## 8. 时间档案 (Archive)

### 📌 功能概述
存放您已经"已读"或"手动归档"，但**未选择保存到文库**的文献。它相当于一个带期限的"回收站"或"历史记录"。

### ⚙️ 详细功能介绍
*   **分页浏览与检索**：支持按期刊来源进行下拉筛选，采用分页加载机制，确保在拥有上万条历史记录时界面依然流畅。
*   **恢复与删除**：
    *   **恢复**：将文献重新放回"最新进展"收件箱中。
    *   **彻底删除**：将该文献从本地数据库中永久抹除。

### 💡 使用示例
某天您突然想起上周似乎扫过一篇关于"单细胞测序"的文章，当时没觉得有用所以归档了。您可以进入"时间档案"，在来源中选择该文章可能发表的期刊，翻页找到该文章后点击"恢复"，再将其保存到文库。

### ⚠️ 注意事项
*   历史文献同样受自动清理策略的影响，到期后会被数据库彻底删除。

---

## 9. 全局搜索 (Search)

### 📌 功能概述
跨全库的全文检索入口，帮助您在收藏的文献海洋中快速定位目标。

### ⚙️ 详细功能介绍
*   输入关键词（匹配标题、摘要、作者等字段），即时返回命中的文献列表。
*   结果可直接操作：保存至文库、归档、翻译或跳转详情页。

---

## 10. 订阅管理 (Subscriptions)

### 📌 功能概述
FolPaper 的数据源配置中心。您可以在这里管理多种类型的订阅源（如 RSS、PubMed、OpenAlex、arXiv），并定制专属的文献信息流。

### ⚙️ 详细功能介绍
*   **多类型订阅源添加**：
    *   **RSS**：输入标准 RSS XML 链接（例如期刊的 RSS 订阅地址）并为其命名。
    *   **PubMed**：输入您的检索词（如 Cancer, Machine Learning），系统将定期自动拉取 PubMed 数据库中匹配该检索词的最新文献。
    *   **OpenAlex**：输入目标期刊的完整名称（如 Nature, Science），系统将通过 OpenAlex 接口定期拉取该期刊发表的最新文献。
    *   **arXiv**：输入分类代码（如 cs.AI），系统将定期拉取该分类下最新发布的预印本。
*   **配置独立抓取与清理策略**：
    *   **抓取天数**：可单独设置每个订阅源每次拉取最近几天的数据。
    *   **保留天数**：可单独设置该订阅源下未读文献在收件箱中保留的天数，该设置优先于系统全局设置生效。
*   **手动单篇定向获取**：如果您已知某篇 arXiv 文章的 ID（如 `2106.09685`），可以在此输入并直接抓取单篇文章到您的文库中，无需等待系统轮询。
*   **OPML / TXT 批量导入导出**：
    *   **导入**：支持将您在 Inoreader、Feedly 等其他阅读器中导出的 `.opml` 或 `.txt` 文件一键导入。
    *   **导出**：将 FolPaper 中的订阅列表导出为 OPML 或 TXT 格式，实现数据自由流转。
*   **编辑与删除**：随时修改订阅源名称、抓取和保留天数，或删除不再关注的订阅源（删除时会同步清理该订阅源下的待处理文献）。

### 💡 使用示例
刚安装 FolPaper 时，您进入此页面，点击"批量导入订阅"，将您积攒多年的文献 RSS 订阅源一次性导入。随后，您希望持续追踪关于"LLM"在医疗领域的最新进展，于是添加了一个 **PubMed** 类型的订阅，名称写为"医疗大模型"，检索词填入 `LLM AND Medical`。由于该领域发展迅速，您将该订阅的"保留天数"设置为 `3` 天，以防止未读列表积压过多。

### ⚠️ 注意事项
*   如果某个 RSS 链接失效或需要特殊的网络环境，后台拉取时可能会失败，您可以在该页面的列表中检查订阅的有效性。
*   对于 OpenAlex 类型的订阅，请确保输入的期刊名称拼写完全正确，否则可能无法匹配到目标期刊。

---

## 11. 系统设置 (Settings)

### 📌 功能概述
全局运行参数配置中心，是 FolPaper 所有 AI 功能（翻译、推荐、调研等）得以正常运转的前提。

### ⚙️ 详细功能介绍
*   **LLM 连接配置**：
    *   **API Key**：您购买或获取的模型授权密钥（保存在本地数据库，不会上传第三方）。
    *   **Base URL**：支持 OpenAI 官方接口或任何兼容 OpenAI 格式的第三方中转接口（如 `https://api.deepseek.com/v1`、阿里云百炼兼容端点）。
*   **模型选择（分任务）**：
    *   **默认模型**：全局兜底模型（百炼推荐 `qwen3.6-flash`）。
    *   **翻译模型 / 推荐·筛选模型 / 调研·综述模型**：可分别为不同任务指定专用模型，留空则回退默认模型。
    *   **推荐并发数**：AI 洞察逐篇判定的并发线程数（默认 20，建议 1~50）。
*   **生成参数**：
    *   **Temperature**：默认创造性（0 确定性最强，2 创造性最高；翻译/洞察建议 0.1~0.3，综述类 0.5~0.7）。
    *   **翻译 Temperature / 调研 Temperature**：分别为翻译和调研任务独立设置。
    *   **超时时间（秒）**：API 请求最长等待时间（默认 60；翻译/综述生成建议 120+）。
    *   **Extra Body (JSON)**：透传给 API 的附加 JSON 参数，如 `{"reasoning_effort": "high"}`；思考模式开关会自动注入 `thinking`/`enable_thinking` 参数，通常无需手动设置。
    *   **思考模式 / Reasoning**：`off`（直接给答案，不展示思考链）/ `auto`（模型自行决定是否思考）/ `on`（强制深度思考）。格式按 base_url 自动适配（百炼 `enable_thinking`、DeepSeek `thinking: {type}`）。
*   **数据源辅助配置**：
    *   **OpenAlex API Key**：可选，提高 OpenAlex 调用限额。
    *   **FlareSolverr 地址**：可选，用于绕过部分网站的 Cloudflare 反爬（如 `http://127.0.0.1:8191/v1`）。
    *   **Crossref 联系邮箱**：可选，填入后调用 Crossref 时带上邮箱标识，提高限流容忍度。
    *   **下载目录**：PDF 全文下载的保存位置（留空默认下载到系统"下载"文件夹）。
*   **测试连通性**：一键测试当前配置的 API 是否能成功连接并回复。

### 💡 使用示例
初次运行软件时，进入"系统设置"。在 Base URL 填入您的服务商地址（如阿里云百炼兼容端点），填入 API Key，模型填入 `qwen3.6-flash`，思考模式保持 `off`。点击"测试 API 连通性"，提示成功后，点击底部的"保存设置"。

### ⚠️ 注意事项
*   API 密钥保存在您的本地数据库中，不会上传至任何第三方服务器。
*   若应用中出现"翻译失败"或"生成失败"等报错，请第一时间来此页面点击"测试连通性"排查问题。
*   数值字段（温度、超时、并发数）留空保存时自动恢复默认值，不会导致配置失效。

---

## 12. 关于 FolPaper (About)

### 📌 功能概述
展示 FolPaper 的版本信息、开发背景及免责声明。软件致力于为科研工作者提供本地化、私密且高效的文献处理工作流。

---

*祝您在 FolPaper 的协助下，科研顺利，成果丰硕！*
